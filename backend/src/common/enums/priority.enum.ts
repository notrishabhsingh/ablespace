/** Priority levels shown in the Figma priority dropdown. */
export enum Priority {
  NO_PRIORITY = 'no_priority',
  URGENT = 'urgent',
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
}

export const PRIORITIES = Object.values(Priority);
